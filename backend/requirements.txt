Django==6.0.4
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
pillow==12.2.0
PyJWT==2.12.1
sqlparse==0.5.5
asgiref==3.11.1

# Production ASGI server (docker-compose/Oracle path)
daphne==4.1.2

# Production WSGI server (Railway path — no WebSocket consumers are used,
# so plain WSGI is simpler and Railway's healthcheck story is more standard)
gunicorn==23.0.0

# Production
whitenoise[brotli]==6.8.2
psycopg2-binary==2.9.10
dj-database-url==2.3.0

# Telegram bot
python-telegram-bot==21.9

# Web push notifications
pywebpush==2.0.0

# Scheduler
apscheduler==3.10.4

# Audit log
django-auditlog==3.0.0

# Testing
pytest-django==4.9.0
pytest-cov==6.1.0

# Environment
python-dotenv==1.1.0

# Excel export
openpyxl==3.1.5

# PDF receipts (payments)
reportlab==4.4.4
